<div class="header">
  <%= button_to "Home", root_path, :method => "get" %>

  <h1>All Categories</h1>
</div>

<ul>
  <% @feed_categories.each do |cat| %>
    <li>
      <%= link_to cat.title, feed_category_path(cat) %><br>
    </li>
  <% end %>
</ul>
